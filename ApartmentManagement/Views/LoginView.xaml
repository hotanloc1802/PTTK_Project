<Window x:Class="ApartmentManagement.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApartmentManagement.Views"
        mc:Ignorable="d"
        Title="Apartment Management Login" Height="403" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF3766CB" Offset="0"/>
                <GradientStop Color="#FF3C47D1" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border Background="White" Width="400" Height="350" CornerRadius="10" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="3"/>
            </Border.Effect>
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Header -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Image Source="/Resource/Assets/Logo-BCons.png" Width="50" Height="50"/>
                    <TextBlock Text="APARTMENT MANAGEMENT" 
                               FontSize="20" FontWeight="Bold" Margin="0,10,0,0" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
                <!-- Username -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Username" Margin="0,0,0,5"/>
                    <Grid>
                        <TextBox x:Name="txtUsername" Height="40" Padding="10,0,0,0" VerticalContentAlignment="Center"/>
                        <TextBlock x:Name="txtUsernameWatermark" Text="Enter your username" 
                                  Foreground="Gray" IsHitTestVisible="False" Padding="12,0,0,0" VerticalAlignment="Center"
                                  Visibility="Visible"/>
                    </Grid>
                </StackPanel>
                <!-- Password -->
                <StackPanel Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Password" Margin="0,0,0,5"/>
                    <Grid>
                        <PasswordBox x:Name="txtPassword" Height="40" Padding="10,0,0,0" VerticalContentAlignment="Center" 
                                    PasswordChanged="txtPassword_PasswordChanged"/>
                        <TextBlock x:Name="txtPasswordWatermark" Text="Enter your password" 
                                  Foreground="Gray" IsHitTestVisible="False" Padding="12,0,0,0" VerticalAlignment="Center"
                                  Visibility="Visible"/>
                    </Grid>
                </StackPanel>

                <!-- Login Button -->
                <Button x:Name="btnLogin" Grid.Row="4" Content="LOGIN" Height="40" 
                        Background="#FF3766CB" Foreground="White" FontWeight="Bold" 
                        Click="btnLogin_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>