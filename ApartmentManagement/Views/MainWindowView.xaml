<Window x:Class="ApartmentManagement.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApartmentManagement.Views"
        mc:Ignorable="d"
        Title="MainWindowView" Height="760" Width="1380" Background="Transparent" ResizeMode="NoResize"
        AllowsTransparency="True" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Add necessary converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Custom ScrollViewer Template -->
        <ControlTemplate x:Key="CustomScrollViewerControlTemplate" TargetType="{x:Type ScrollViewer}">
            <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Rectangle x:Name="Corner" Grid.Column="1" Fill="Transparent" Grid.Row="1"/>
                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                  CanContentScroll="{TemplateBinding CanContentScroll}" 
                                  CanHorizontallyScroll="False"
                                  CanVerticallyScroll="False" 
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  Content="{TemplateBinding Content}" 
                                  Grid.Column="0" 
                                  Margin="{TemplateBinding Padding}"
                                  Grid.Row="0"/>
                <ScrollBar x:Name="PART_VerticalScrollBar" 
                     AutomationProperties.AutomationId="VerticalScrollBar"
                     Cursor="Arrow" 
                     Grid.Column="1" 
                     Maximum="{TemplateBinding ScrollableHeight}"
                     Minimum="0" 
                     Grid.Row="0" 
                     Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                     Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                     ViewportSize="{TemplateBinding ViewportHeight}" 
                     Style="{DynamicResource CustomScrollBarStyle}"/>
                <ScrollBar x:Name="PART_HorizontalScrollBar" 
                     AutomationProperties.AutomationId="HorizontalScrollBar"
                     Cursor="Arrow" 
                     Grid.Column="0" 
                     Maximum="{TemplateBinding ScrollableWidth}"
                     Minimum="0" 
                     Orientation="Horizontal" 
                     Grid.Row="1"
                     Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                     Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                     ViewportSize="{TemplateBinding ViewportWidth}" 
                     Style="{DynamicResource CustomScrollBarStyle}"/>
            </Grid>
        </ControlTemplate>

        <!-- Stats Card Style -->
        <Style x:Key="statsCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0 0 20 20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Direction="270" BlurRadius="10" Opacity="0.1" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0,0,0,15" HorizontalAlignment="Center" Width="1300" Height="686" VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="155"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="0.01*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>
        <Border CornerRadius="20 0 0 20" ClipToBounds="True" Background="White" Grid.Row="0"  Grid.RowSpan="3" IsEnabled="False">
            <Border  CornerRadius="20 0 0 20" ClipToBounds="True" Margin="0,0,-5,0">
                <Border.Background>
                    <SolidColorBrush Color="#0430AD"></SolidColorBrush>
                </Border.Background>
            </Border>
        </Border>
        <Grid Grid.Column ="0" Grid.Row ="0">
            <Image  Stretch="Uniform" Width="60" HorizontalAlignment="Center" />
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1" Background="#f1f1f1" Margin="10,0,0,0" >
        </Grid>
        <Grid Grid.Column="0" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ListBox  Margin="0,20,0,20" BorderThickness="0" Background="Transparent" SelectedIndex="0" ScrollViewer.VerticalScrollBarVisibility="Disabled"       >
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <!-- Item 1 -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2.5*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="0.01*" />
                    </Grid.RowDefinitions>
                    <Ellipse Width="80" Height="80">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Grid Grid.Row="2" Background="#f1f1f1" Margin="10,0,10,0" Height="1" />
                    <Grid Grid.Row="1">
                        <TextBlock Name="BconsMienDong"  Text="Miền Đông" Tag="mien_dong" FontFamily="Montserrat Bold"
FontSize="13" Foreground="White" VerticalAlignment="Center"
Height="18" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>
                <!-- Item 2 -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2.5*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="0.01*" />
                    </Grid.RowDefinitions>
                    <Ellipse Width="80" Height="80">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Grid Grid.Row="2" Background="#f1f1f1" Margin="10,0,10,0" Height="1" />
                    <Grid Grid.Row="1">
                        <TextBlock Name="BconsPlaza" Text="Plaza" Tag="plaza" FontFamily="Montserrat Bold"
       FontSize="13" Foreground="White" VerticalAlignment="Center"
       Height="18" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>

                <!-- Item 3 -->
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2.5*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="0.01*" />
                    </Grid.RowDefinitions>
                    <Ellipse Width="80" Height="80">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Grid Grid.Row="2" Background="#f1f1f1" Margin="10,0,10,0" Height="1" />
                    <Grid Grid.Row="1">
                        <TextBlock Text="Sala" Tag="sala" FontFamily="Montserrat Bold"
       FontSize="13" Foreground="White" VerticalAlignment="Center"
       Height="18" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>

                <!-- Item 4 -->
                <Grid Grid.Row="3" Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2.5*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="0.01*" />
                    </Grid.RowDefinitions>
                    <Ellipse Width="80" Height="80">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Grid Grid.Row="2" Background="#f1f1f1" Margin="10,0,10,0" Height="1" />
                    <Grid Grid.Row="1">
                        <TextBlock Text="Green View" Tag="green_view" FontFamily="Montserrat Bold"
       FontSize="13" Foreground="White" VerticalAlignment="Center"
       Height="18" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>
            </ListBox>
            <Grid Grid.Row="1">
                <Ellipse Width="50" Height="50" Stroke="White" StrokeThickness="1" StrokeDashArray="10,2" Fill="Transparent" VerticalAlignment="Top"/>
                <iconPacks:PackIconMaterial Kind="Plus" Width="17" Height="17"
           Foreground="#ebedf3" Margin="42,16,0,0"/>
            </Grid>
        </Grid>
        <Border Background="#0430AD" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="3"/>
        <Border CornerRadius="20 0 0 20" Background="White" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="3"/>
        <Border Background="#F3F5F7" Grid.Column="2" Grid.ColumnSpan="2" Grid.RowSpan="3"/>
        <Grid Grid.Column="1" Grid.Row="0" Margin="10,0,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Ellipse Width="55" Height="55">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>
            <Grid Grid.Column="1">
                <TextBlock x:Name="Bcons" Text="Miền Đông" FontFamily="Montserrat Bold" FontSize="13" Foreground="Black" VerticalAlignment="Center" Height="18"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1" Background="#f1f1f1" Margin="0,0,0,0" >
        </Grid>
        <Grid Grid.Column="1" Grid.Row="2">
            <StackPanel Margin="0,30,0,0" >
                <Border CornerRadius="10" Padding="5,5,5,5" Margin="0,0,0,0" Width="142"  >
                    <Button x:Name="btnDashboard"  Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5" Background="#0430AD" >

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="HomeVariant" VerticalAlignment="Center" Foreground="white"/>
                            <!-- Gán tên cho TextBlock để có thể sử dụng trong Storyboard -->
                            <TextBlock x:Name="dashboardText" Text=" Dashboard" FontFamily="Montserrat Bold" FontSize="13" Foreground="white" VerticalAlignment="Center" Height="18" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" Padding="5,5,5,5"  Width="142"  >
                    <Button x:Name="btnApartment" Style="{StaticResource menuButton}"   Width="138" Margin="-5,5,0,5" Click="BtnApartment_Click" >
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetName="apartmentText" 
            Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
            To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation 
            Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation 
             Storyboard.TargetName="apartmentText" 
             Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
             To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="Domain" VerticalAlignment="Center" Foreground="#959595"/>
                            <TextBlock  x:Name="apartmentText" Text=" Apartment" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595"
 VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" CornerRadius="10" Padding="5,5,5,5"  Width="142"  >
                    <Button x:Name="btnResident" Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5" Click="BtnResident_Click" >
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetName="residentText" 
            Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
            To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation 
            Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation 
            Storyboard.TargetName="residentText" 
            Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
            To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="AccountGroup" VerticalAlignment="Center" Foreground="#959595"/>
                            <TextBlock  x:Name="residentText"  Text="Resident" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595"
 VerticalAlignment="Center" Margin="3,0,0,0"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" Padding="5,5,5,5"  Width="142">
                    <Button x:Name="btnPayement"  Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5" Click="BtnPayment_Click">
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation 
            Storyboard.TargetName="paymentText" 
            Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
            To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation 
             Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
            To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation 
            Storyboard.TargetName="paymentText" 
            Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
            To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="CreditCardOutline"  VerticalAlignment="Center" Foreground="#959595"/>
                            <TextBlock x:Name="paymentText"  Text=" Payment" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" Padding="5,5,5,5"  Width="142">
                    <Button x:Name="btnService"  Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5" Click="BtnService_Click">
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation 
Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation 
Storyboard.TargetName="serviceText" 
Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation 
 Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation 
Storyboard.TargetName="serviceText" 
Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="ToolboxOutline"  VerticalAlignment="Center" Foreground="#959595" Margin="0,0,3,0"/>
                            <TextBlock x:Name="serviceText"  Text="Service" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" Padding="5,5,5,5"  Width="142">
                    <Button x:Name="btnSetting2"  Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5">
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation 
Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation 
Storyboard.TargetName="settingText" 
Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation 
 Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" 
To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation 
Storyboard.TargetName="settingText" 
Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="Cog"  VerticalAlignment="Center" Foreground="#959595"/>
                            <TextBlock x:Name="settingText"  Text=" Setting" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border Background="Transparent" Padding="5,5,5,5"  Width="142">
                    <Button x:Name="btnlogout"  Style="{StaticResource menuButton}"  Width="138" Margin="-5,5,0,5">
                        <Button.Triggers>
                            <!-- Trigger khi di chuột vào Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Đổi màu nền của Button khi di chuột vào -->
                                        <ColorAnimation Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" To="#CDDFFB" Duration="0:0:0.2" />
                                        <!-- Đổi màu Foreground của TextBlock khi di chuột vào -->
                                        <ColorAnimation Storyboard.TargetName="settingText" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" To="Black" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Trigger khi di chuột ra khỏi Button -->
                            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- Trở lại màu nền gốc của Button khi di chuột ra -->
                                        <ColorAnimation Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" To="Transparent" Duration="0:0:0.2" />
                                        <!-- Trở lại màu Foreground gốc của TextBlock khi di chuột ra -->
                                        <ColorAnimation Storyboard.TargetName="settingText" Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" To="#959595" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Icon:PackIconMaterial Kind="ExitToApp"  VerticalAlignment="Center" Foreground="#959595"/>
                            <TextBlock x:Name="logout"  Text=" Log out" FontFamily="Montserrat Bold" FontSize="13" Foreground="#959595" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Border>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid Margin="30,0,0,0">
                <Border x:Name="boSearch" Background="#EEF0F2" CornerRadius="10" Height="45" HorizontalAlignment="Center" Margin="0,16,0,15" Width="597" >
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Icon:PackIconMaterial Kind="Magnify" Foreground="#9FA4AD" VerticalAlignment="Center" Margin="7,0,0,0" Width="18" Height="18"/>
                        <TextBox x:Name="boxSearch" Background="Transparent" BorderBrush="Transparent" Foreground="#9FA4AD" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" 
                         Width="571" VerticalAlignment="Center" Padding="5,5" />
                    </StackPanel>
                </Border>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtSearch" Text="Search..." Foreground="#9FA4AD" FontFamily="Montserrat" Margin="30,0,0,0" IsHitTestVisible="False"  />
                </StackPanel>
            </Grid>
            <Grid Grid.Column="1" Margin="30,0,30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="0.02*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <iconPacks:PackIconMaterial Kind="CalendarOutline" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#0430AD"/>
                </Grid>
                <Grid Grid.Column="1">
                    <iconPacks:PackIconMaterial Kind="MessageTextOutline" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#0430AD"/>
                </Grid>
                <Grid Grid.Column="2">
                    <iconPacks:PackIconMaterial Kind="BellOutline" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#0430AD"/>
                </Grid>
                <Grid Grid.Column="3" Background="#9FA4AD" Margin="0,23,0,23"  >
                </Grid>
                <Grid Grid.Column="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Ellipse Width="40" Height="40">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Resource/Assets/img1.jpg"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </Grid>
                    <Grid Grid.Column="1">
                        <TextBlock x:Name="User" Text="Tuan cui" FontFamily="Montserrat Bold" FontSize="18" Foreground="Black" VerticalAlignment="Center" Height="18"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Column="2" Grid.Row="1" Background="white" Margin="0,0,10,0">
        </Grid>
        <Grid Grid.Column="2" Grid.Row="2"  Margin="0,0,10,0">
            <Grid>
                <!-- ScrollViewer for content -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Disabled"
                 Template="{StaticResource CustomScrollViewerControlTemplate}">

                    <!-- Main Section -->
                    <Grid Grid.Row="2" Margin="30,0,20,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                        </Grid>

                        <!-- Title and filter tabs -->
                        <TextBlock Text="Dashboard"  Foreground="#434343"  FontFamily="Montserrat Bold" Grid.Row="0" FontSize="20"  Margin="0,15,0,10"/>

                        <!-- Tab buttons -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0">
                            <Border x:Name="borderAll" CornerRadius="10"   Margin="0,0,5,0"  Background="#0430AD" BorderThickness="0" Width="60">
                                <Button x:Name="btnAll" Content="All" Foreground="White"  FontFamily="Montserrat Bold"/>
                            </Border>
                        </StackPanel>

                        <!-- Separator -->
                        <Separator Grid.Row="3" Margin="0,15"/>

                        <!-- Stats Cards -->
                        <Grid Grid.Row="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Occupancy Stats Card -->
                            <Border Grid.Column="0" Style="{StaticResource statsCard}" Margin="0 0 10 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                        <Border Background="#E6F1FF" CornerRadius="5" Padding="8">
                                            <Icon:PackIconMaterial Kind="HomeCity" Width="22" Height="22" Foreground="#0430AD"/>
                                        </Border>
                                        <TextBlock Text="Total Occupancy" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="#121518" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                                        <TextBlock x:Name="OccupancyCount" Text="{Binding ApartmentOccupiedCount,StringFormat={}{0}/}" FontSize="28" FontWeight="Bold" 
                                       Foreground="#121518"/>
                                        <TextBlock  Text="{Binding ApartmentAllCount}" FontSize="20" Foreground="#6C7682" 
                                       VerticalAlignment="Bottom" Margin="5,0,0,3"/>
                                        <TextBlock Text="apartments" FontSize="14" Foreground="#6C7682" 
                                       VerticalAlignment="Bottom" Margin="5,0,0,3"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Background="#E6FFE9" CornerRadius="5" Padding="5,2">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="ArrowUp" Width="12" Height="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding ChangeInOccupiedRate,StringFormat={}{0:2.##}%}" FontSize="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Text="vs last month" FontSize="12" Foreground="#6C7682" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Revenue Stats Card -->
                            <Border Grid.Column="1" Style="{StaticResource statsCard}" Margin="10 0 10 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                        <Border Background="#E6F1FF" CornerRadius="5" Padding="8">
                                            <Icon:PackIconMaterial Kind="CurrencyUsd" Width="22" Height="22" Foreground="#0430AD"/>
                                        </Border>
                                        <TextBlock Text="Total Revenue" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="#121518" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                    </StackPanel>

                                    <StackPanel  Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                                        <TextBlock  x:Name="TotalRevenue" Text="{Binding TotalRevenue, StringFormat={}{0:0.##}/}" FontSize="28" FontWeight="Bold" Foreground="#121518"/>
                                        <TextBlock  Text="{Binding TotalRevenue,StringFormat={}{0:0.##}}" FontSize="20" FontWeight="Bold" Foreground="#6C7682"  Margin="5,8,0,3"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Background="#E6FFE9" CornerRadius="5" Padding="5,2">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="ArrowUp" Width="12" Height="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding ChangeInRevenue,StringFormat={}{0:2.##}%}" FontSize="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Text="vs last month" FontSize="12" Foreground="#6C7682" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Residents Stats Card -->
                            <Border Grid.Column="2" Style="{StaticResource statsCard}" Margin="10 0 10 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                        <Border Background="#E6F1FF" CornerRadius="5" Padding="8">
                                            <Icon:PackIconMaterial Kind="AccountGroup" Width="22" Height="22" Foreground="#0430AD"/>
                                        </Border>
                                        <TextBlock Text="Total Residents" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="#121518" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1" x:Name="TotalResidents" Text="{Binding ResidentAllCount}" FontSize="28" 
                                   FontWeight="Bold" Foreground="#121518"/>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Background="#E6FFE9" CornerRadius="5" Padding="5,2">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="ArrowUp" Width="12" Height="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding ChangeInResidentRate, StringFormat={}{0:2.##}%}" FontSize="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Text="vs last month" FontSize="12" Foreground="#6C7682" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Service Requests Stats Card -->
                            <Border Grid.Column="3" Style="{StaticResource statsCard}" Margin="10 0 0 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                        <Border Background="#E6F1FF" CornerRadius="5" Padding="8">
                                            <Icon:PackIconMaterial Kind="Tools" Width="22" Height="22" Foreground="#0430AD"/>
                                        </Border>
                                        <TextBlock Text="Service Requests" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="#121518" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                                        <TextBlock x:Name="PendingServices" Text="{Binding RequestAllCount}" FontSize="28" FontWeight="Bold" 
                                       Foreground="#121518"/>
                                        <TextBlock Text=" Pending" FontSize="16" Foreground="#6C7682" 
                                       VerticalAlignment="Bottom" Margin="5,0,0,3"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Background="#FFE6E6" CornerRadius="5" Padding="5,2">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="ArrowUp" Width="12" Height="12" Foreground="#F44336" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding ChangeInRequestRate, StringFormat={}{0:2.##}%}" FontSize="12" Foreground="#F44336" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Text="vs last month" FontSize="12" Foreground="#6C7682" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Chart Section Title -->
                        <Grid Grid.Row="5" Margin="0 20 0 15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Performance Analytics" FontSize="20" FontWeight="SemiBold" Foreground="#121518"/>

                            <!-- Filter Dropdown -->
                            <ComboBox Grid.Column="1" Width="150" Height="35" SelectedIndex="0" VerticalContentAlignment="Center" Padding="10 0">
                                <ComboBoxItem Content="Last 6 Months"/>
                                <ComboBoxItem Content="Last Year"/>
                                <ComboBoxItem Content="All Time"/>
                            </ComboBox>
                        </Grid>

                        <!-- Chart Section -->
                        <Grid Grid.Row="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Revenue Chart -->
                            <Border Grid.Column="0" Style="{StaticResource statsCard}" Margin="0 0 10 20">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="Revenue Trend" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#121518" Margin="0 0 0 15"/>

                                    <!-- Placeholder for actual chart - will be replaced by actual LiveCharts control -->
                                    <Grid Grid.Row="1" Height="250" Background="#f9f9f9">
                                        <!-- Path simulation of a chart -->
                                        <Path Stroke="#0430AD" StrokeThickness="3" Margin="30 20" 
                                  Data="M0,200 C20,180 40,170 60,160 C80,150 100,130 120,110 C140,90 160,80 180,70 C200,60 220,50 240,40 C260,30 280,25 300,20" 
                                  Fill="Transparent"/>

                                        <!-- X-Axis labels -->
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 0 0 10">
                                            <TextBlock Text="Jan" Margin="45,0" Foreground="#6C7682"/>
                                            <TextBlock Text="Feb" Margin="45,0" Foreground="#6C7682"/>
                                            <TextBlock Text="Mar" Margin="45,0" Foreground="#6C7682"/>
                                            <TextBlock Text="Apr" Margin="45,0" Foreground="#6C7682"/>
                                            <TextBlock Text="May" Margin="45,0" Foreground="#6C7682"/>
                                            <TextBlock Text="Jun" Margin="45,0" Foreground="#6C7682"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>

                            <!-- Occupancy Donut Chart -->
                            <Border Grid.Column="1" Style="{StaticResource statsCard}" Margin="10 0 0 20">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="Building Occupancy" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#121518" Margin="0 0 0 15"/>

                                    <!-- Placeholder for donut chart -->
                                    <Viewbox Grid.Row="1" Margin="10">
                                        <Grid Width="180" Height="180">
                                            <!-- Simulated donut chart with colored arcs -->
                                            <Ellipse Width="180" Height="180" Fill="#f5f5f5"/>
                                            <Path Fill="#0430AD" Stretch="None" 
                                    Data="M90,0 A90,90 0 0,1 180,90 L90,90 Z"/>
                                            <Path Fill="#62A1FF" Stretch="None" 
                                    Data="M180,90 A90,90 0 0,1 90,180 L90,90 Z"/>
                                            <Path Fill="#FFB74D" Stretch="None" 
                                    Data="M90,180 A90,90 0 0,1 0,90 L90,90 Z"/>
                                            <Path Fill="#78C2AD" Stretch="None" 
                                    Data="M0,90 A90,90 0 0,1 90,0 L90,90 Z"/>

                                            <!-- Inner circle for donut effect -->
                                            <Ellipse Width="90" Height="90" Fill="White"/>
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="81%" FontSize="24" FontWeight="Bold" 
                                            Foreground="#121518" HorizontalAlignment="Center"/>
                                                <TextBlock Text="Occupancy" FontSize="12" 
                                            Foreground="#6C7682" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </Viewbox>

                                    <!-- Chart legend -->
                                    <StackPanel Grid.Row="2" Margin="0 10 0 0">
                                        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                                            <Rectangle Width="14" Height="14" Fill="#0430AD" Margin="0 0 10 0"/>
                                            <TextBlock Text="Miền Đông" FontSize="12" Foreground="#121518" Width="100"/>
                                            <TextBlock Text="85%" FontWeight="SemiBold" FontSize="12" Foreground="#121518"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                                            <Rectangle Width="14" Height="14" Fill="#62A1FF" Margin="0 0 10 0"/>
                                            <TextBlock Text="Plaza" FontSize="12" Foreground="#121518" Width="100"/>
                                            <TextBlock Text="78%" FontWeight="SemiBold" FontSize="12" Foreground="#121518"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                                            <Rectangle Width="14" Height="14" Fill="#FFB74D" Margin="0 0 10 0"/>
                                            <TextBlock Text="Sala" FontSize="12" Foreground="#121518" Width="100"/>
                                            <TextBlock Text="82%" FontWeight="SemiBold" FontSize="12" Foreground="#121518"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Rectangle Width="14" Height="14" Fill="#78C2AD" Margin="0 0 10 0"/>
                                            <TextBlock Text="Green View" FontSize="12" Foreground="#121518" Width="100"/>
                                            <TextBlock Text="80%" FontWeight="SemiBold" FontSize="12" Foreground="#121518"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Recent Activity Section -->
                        <Grid Grid.Row="7">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Recent Activity -->
                            <Border Grid.Column="0" Style="{StaticResource statsCard}" Margin="0 0 10 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="Recent Activity" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#121518" Margin="0 0 0 15"/>

                                    <StackPanel Grid.Row="1">
                                        <!-- Activity 1 -->
                                        <Border Padding="10" BorderThickness="0 0 0 1" BorderBrush="#EEEEEE">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Background="#E6F1FF" CornerRadius="5" Padding="8" Margin="0 0 15 0">
                                                    <Icon:PackIconMaterial Kind="CreditCard" Width="18" Height="18" Foreground="#0430AD"/>
                                                </Border>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="New Payment Received" FontWeight="SemiBold" 
                                                   FontSize="14" Foreground="#121518"/>
                                                    <TextBlock Text="Apartment D04.07 - $1,200.00" 
                                                   FontSize="12" Foreground="#6C7682" Margin="0 3 0 0"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="10 min ago" 
                                               FontSize="12" Foreground="#B0B9C6" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Activity 2 -->
                                        <Border Padding="10" BorderThickness="0 0 0 1" BorderBrush="#EEEEEE">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Background="#FFE6E6" CornerRadius="5" Padding="8" Margin="0 0 15 0">
                                                    <Icon:PackIconMaterial Kind="Tools" Width="18" Height="18" Foreground="#F44336"/>
                                                </Border>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="New Service Request" FontWeight="SemiBold" 
                                                   FontSize="14" Foreground="#121518"/>
                                                    <TextBlock Text="Apartment A05.09 - Plumbing Issue" 
                                                   FontSize="12" Foreground="#6C7682" Margin="0 3 0 0"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="1 hour ago" 
                                               FontSize="12" Foreground="#B0B9C6" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Activity 3 -->
                                        <Border Padding="10" BorderThickness="0 0 0 1" BorderBrush="#EEEEEE">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Background="#E6FFE9" CornerRadius="5" Padding="8" Margin="0 0 15 0">
                                                    <Icon:PackIconMaterial Kind="AccountPlus" Width="18" Height="18" Foreground="#4CAF50"/>
                                                </Border>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="New Resident" FontWeight="SemiBold" 
                                                   FontSize="14" Foreground="#121518"/>
                                                    <TextBlock Text="Apartment C06.11 - Nguyen Van A" 
                                                   FontSize="12" Foreground="#6C7682" Margin="0 3 0 0"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="3 hours ago" 
                                               FontSize="12" Foreground="#B0B9C6" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>

                                        <!-- Activity 4 -->
                                        <Border Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Background="#E6F1FF" CornerRadius="5" Padding="8" Margin="0 0 15 0">
                                                    <Icon:PackIconMaterial Kind="Check" Width="18" Height="18" Foreground="#0430AD"/>
                                                </Border>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Service Request Completed" FontWeight="SemiBold" 
                                                   FontSize="14" Foreground="#121518"/>
                                                    <TextBlock Text="Apartment B02.03 - Electrical Issue" 
                                                   FontSize="12" Foreground="#6C7682" Margin="0 3 0 0"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="6 hours ago" 
                                               FontSize="12" Foreground="#B0B9C6" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Quick Links and Reminders -->
                            <Border Grid.Column="1" Style="{StaticResource statsCard}" Margin="10 0 0 0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="Quick Actions" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="#121518" Margin="0 0 0 15"/>

                                    <StackPanel Grid.Row="1">
                                        <Button Margin="0 0 0 10" Height="40" Background="#0430AD" Foreground="White" 
                                    BorderThickness="0" FontWeight="SemiBold" FontSize="13" VerticalContentAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="CreditCard" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Create New Payment" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>

                                        <Button Margin="0 0 0 10" Height="40" Background="#4CAF50" Foreground="White" 
                                    BorderThickness="0" FontWeight="SemiBold" FontSize="13" VerticalContentAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="AccountPlus" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Add New Resident" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>

                                        <Button Margin="0 0 0 10" Height="40" Background="#FFB74D" Foreground="White" 
                                    BorderThickness="0" FontWeight="SemiBold" FontSize="13" VerticalContentAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="FileDocument" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Generate Reports" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>

                                        <Button Margin="0 0 0 10" Height="40" Background="#F44336" Foreground="White" 
                                    BorderThickness="0" FontWeight="SemiBold" FontSize="13" VerticalContentAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="Tools" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="View Service Requests" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>

                                        <Button Height="40" Background="#78C2AD" Foreground="White" 
                                    BorderThickness="0" FontWeight="SemiBold" FontSize="13" VerticalContentAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <Icon:PackIconMaterial Kind="Email" Width="18" Height="18" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Send Notifications" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>